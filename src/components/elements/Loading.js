import React from "react";
import { brandColors } from "../../utils/utilities";

import Loader from "react-loader-spinner";

const Loading = () => (
  <Loader type="Puff" color="#8E79FC" height={100} width={100} />
);

export default Loading;
